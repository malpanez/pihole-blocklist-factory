---
phase: 03-profile-features-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/blocklist_builder/types.py
  - src/blocklist_builder/config.py
  - config/profiles.yml
  - tests/test_config.py
  - tests/test_build.py
  - tests/test_build_internal.py
  - tests/test_recommend.py
  - tests/test_analyze.py
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04

must_haves:
  truths:
    - "Profile dataclass has only `name` and `include_categories` fields"
    - "Policies dataclass has only `category_precedence`, `core_domains`, and `base_allowlist` fields"
    - "config/profiles.yml contains no include_sources, exclude_sources, or strict keys"
    - "All tests pass with no TypeError on dataclass construction"
    - "Coverage remains at 99%+ and ruff check is clean"
  artifacts:
    - path: "src/blocklist_builder/types.py"
      provides: "Cleaned Profile dataclass"
      contains: "class Profile"
    - path: "src/blocklist_builder/config.py"
      provides: "Cleaned Policies dataclass and parse logic"
      contains: "class Policies"
    - path: "config/profiles.yml"
      provides: "Clean YAML with no dead fields"
  key_links:
    - from: "src/blocklist_builder/config.py"
      to: "src/blocklist_builder/types.py"
      via: "Profile() and Policies() constructor calls"
      pattern: "Profile\\(|Policies\\("
    - from: "tests/test_config.py"
      to: "src/blocklist_builder/config.py"
      via: "load_settings integration test"
      pattern: "load_settings"
---

<objective>
Remove four dead fields from Profile and Policies dataclasses, clean YAML configs, and update all test fixtures.

Purpose: These fields (include_sources, exclude_sources, strict, sensitive_domains) are parsed from YAML but never read by the build pipeline. Removing them eliminates dead code that misleads maintainers into thinking per-device profile differentiation exists.

Output: Clean dataclasses, clean YAML, passing tests with no behavior change to dist/ output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-profile-features-cleanup/03-RESEARCH.md
@.planning/phases/03-profile-features-cleanup/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead fields from dataclasses and config parser (PROF-01, PROF-02)</name>
  <files>src/blocklist_builder/types.py, src/blocklist_builder/config.py</files>
  <read_first>src/blocklist_builder/types.py, src/blocklist_builder/config.py, .planning/phases/03-profile-features-cleanup/03-RESEARCH.md</read_first>
  <action>
In `src/blocklist_builder/types.py`:
- Remove lines 52-54 from the `Profile` dataclass: the `include_sources`, `exclude_sources`, and `strict` field definitions. The resulting `Profile` should have only `name` and `include_categories`.

In `src/blocklist_builder/config.py`:
- Remove line 30: `sensitive_domains: set[str] | None = None` from the `Policies` dataclass. The resulting `Policies` should have only `category_precedence`, `core_domains`, and `base_allowlist`.
- Remove line 111: `sensitive_domains=set(policies_data.get("sensitive_domains", []) or []),` from the `Policies()` constructor call.
- Remove lines 121-123 from the `Profile()` constructor call: the `include_sources=`, `exclude_sources=`, and `strict=` keyword arguments. The resulting `Profile()` call should have only `name=` and `include_categories=`.

After edits, run `uv run ruff check src/blocklist_builder/types.py src/blocklist_builder/config.py` to catch any orphaned imports.
  </action>
  <verify>
    <automated>uv run python -c "from blocklist_builder.types import Profile; p = Profile(name='test'); assert not hasattr(p, 'include_sources'); assert not hasattr(p, 'strict'); print('Profile OK')" && uv run python -c "from blocklist_builder.config import Policies; p = Policies(category_precedence=[], core_domains=set(), base_allowlist=set()); assert not hasattr(p, 'sensitive_domains'); print('Policies OK')"</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "include_sources\|exclude_sources\|strict" src/blocklist_builder/types.py` returns no hits
- `grep -n "sensitive_domains" src/blocklist_builder/config.py` returns no hits
- `grep -n "include_sources\|exclude_sources\|strict" src/blocklist_builder/config.py` returns no hits (in the Profile constructor)
- `uv run ruff check src/blocklist_builder/types.py src/blocklist_builder/config.py` passes clean
  </acceptance_criteria>
  <done>Profile has only name + include_categories. Policies has only category_precedence + core_domains + base_allowlist. No parse logic references removed fields. Ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Clean YAML config files (PROF-03)</name>
  <files>config/profiles.yml</files>
  <read_first>config/profiles.yml, .planning/phases/03-profile-features-cleanup/03-RESEARCH.md</read_first>
  <action>
In `config/profiles.yml`:
- Remove the header comment block (lines 1-6) that documents `include_sources`, `exclude_sources`, and `strict` field semantics.
- For each of the 7 profiles (base, standard, strict, relaxed, android, ios, windows): remove the `include_sources: []`, `exclude_sources: []`, and `strict: false` (or `strict: true`) keys. Each profile should retain only `include_categories`.

No changes needed to `config/policies.yml` -- research confirmed `sensitive_domains` is NOT present in that file.

After editing, verify with grep that no references to removed fields remain.
  </action>
  <verify>
    <automated>grep -c "include_sources\|exclude_sources\|strict" config/profiles.yml; test $? -eq 1 && echo "CLEAN" || echo "STILL DIRTY"</automated>
  </verify>
  <acceptance_criteria>
- `grep "include_sources\|exclude_sources\|strict" config/profiles.yml` returns exit code 1 (no matches)
- `grep "sensitive_domains" config/policies.yml` returns exit code 1 (no matches -- was already absent)
- YAML is valid: `uv run python -c "import yaml; yaml.safe_load(open('config/profiles.yml'))"`
  </acceptance_criteria>
  <done>config/profiles.yml contains no references to include_sources, exclude_sources, or strict. All 7 profiles have only include_categories. YAML parses without error.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests to remove dead field references (PROF-04)</name>
  <files>tests/test_config.py, tests/test_build.py, tests/test_build_internal.py, tests/test_recommend.py, tests/test_analyze.py</files>
  <read_first>tests/test_config.py, tests/test_build.py, tests/test_build_internal.py, tests/test_recommend.py, tests/test_analyze.py</read_first>
  <action>
This task depends on Task 1 and Task 2 completing first.

In `tests/test_config.py`:
- Line 66: Remove `sensitive_domains: [sensitive.example]` from the inline policies YAML fixture string.
- Lines 78-80: Remove `include_sources: []`, `exclude_sources: []`, `strict: true` from the inline profiles YAML fixture string.
- Line 91: Remove the assertion `assert settings.profiles.by_name["default"].strict is True`.

In `tests/test_build.py`:
- Line 28: Remove `sensitive_domains=set(),` from the `Policies()` constructor call.

In `tests/test_build_internal.py`:
- Line 23: Remove `sensitive_domains=set(),` from the first `Policies()` constructor call.
- Line 129: Remove `sensitive_domains=set(),` from the second `Policies()` constructor call.

In `tests/test_recommend.py`:
- Line 28: Remove `sensitive_domains=set(),` from the `Policies()` constructor call.

In `tests/test_analyze.py`:
- Line 28: Remove `sensitive_domains=set(),` from the `Policies()` constructor call.

After all edits, run the full test suite with coverage and ruff.
  </action>
  <verify>
    <automated>uv run pytest --cov=blocklist_builder --cov-report=term-missing -q && uv run ruff check src/ tests/</automated>
  </verify>
  <acceptance_criteria>
- `grep -rn "sensitive_domains" tests/` returns no hits
- `grep -rn "\.strict" tests/test_config.py` returns no hits for the removed assertion
- `uv run pytest --cov=blocklist_builder --cov-report=term-missing -q` shows all tests pass with coverage >= 99%
- `uv run ruff check src/ tests/` passes clean
  </acceptance_criteria>
  <done>All 5 test files updated. No test constructs Profile or Policies with removed fields. Full suite passes. Coverage >= 99%. Ruff clean.</done>
</task>

</tasks>

<verification>
Phase-level verification after all tasks complete:

1. **No dead field references anywhere in source or config:**
   ```
   grep -rn "include_sources\|exclude_sources\|sensitive_domains" src/ config/ tests/ | grep -v "\.md:" | grep -v "__pycache__"
   ```
   Should return only `include_categories` hits (not `include_sources`).

2. **No `strict` field references in source/config/tests** (excluding documentation):
   ```
   grep -rn "strict" src/blocklist_builder/types.py src/blocklist_builder/config.py config/profiles.yml tests/test_config.py
   ```
   Should return no hits.

3. **Full test suite + coverage + lint:**
   ```
   uv run pytest --cov=blocklist_builder --cov-report=term-missing -q && uv run ruff check src/ tests/
   ```
</verification>

<success_criteria>
- Profile dataclass: 2 fields only (name, include_categories)
- Policies dataclass: 3 fields only (category_precedence, core_domains, base_allowlist)
- config/profiles.yml: no include_sources, exclude_sources, or strict keys
- All tests pass, coverage >= 99%, ruff clean
- No behavior change to build pipeline output
</success_criteria>

<output>
After completion, create `.planning/phases/03-profile-features-cleanup/03-01-SUMMARY.md`
</output>
