profiles:
  base:
    include_categories: ["advertising", "tracking", "malicious"]

  security:
    include_categories: ["malicious"]

  aggressive:
    include_categories: ["advertising", "tracking", "malicious", "telemetry"]

  # Perfiles por OS/dispositivo (ejemplos)
  android:
    include_categories: ["advertising", "tracking", "malicious"]

  ios:
    include_categories: ["advertising", "tracking", "malicious"]

  windows:
    include_categories: ["advertising", "tracking", "malicious", "telemetry"]

  macos:
    include_categories: ["advertising", "tracking", "malicious"]
